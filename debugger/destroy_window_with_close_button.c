#include "debugger.h"


void destroy_window_with_close_button(void)
{
    deb_printer("Good Flag <[133]> ? Destroy Window Using Close Button ...\nFree Used Memory \nKill Process ...", (int)PDATA_TO_OPENED_TTY);
    exit(GOODFLAG);
}